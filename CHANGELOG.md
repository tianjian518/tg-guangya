# 更新日志

## v1.2.2（2026-09-04）

- **纠正 v1.2.1 覆盖不全：单文件同样重复**（不只文件夹）。根因是两类更基础的问题：
  1. **长文件名剥不净**：MoviePilot/压制组自动下载器落盘名如
     `白夜追凶 (2017){tv tmdb-73982}[S01-S02][2160p][HEVC][AAC][中字][2.0](67.7GB 61个文件)`，
     旧的 `title_core` 剥不掉 `{tmdb 标签}`/`[S01-S02]`/体积与文件数尾巴，核心名残留一长串
     数字字母 → `names_match` 把同一部片判成不同片 → 云端复查查不到 → 单文件/文件夹都复制副本。
     现在标题归一化会先剥掉这类结构性尾巴再比对。
  2. **复查只查分类目录**：分类目录下没有时，从不去查**转存根目录**——旧版本/未分类/其它
     工具平铺在根目录的资源永远看不见，照样重转。现在云端复查 = 分类目录 + 转存根目录都查。
- 测试新增：MoviePilot 长名文件夹/单文件在根目录、同片新磁力 → 跳过（21 e2e + 15 匹配 + 6 评分全绿）。

## v1.2.1（2026-09-04）

- **修复云端去重形同虚设（重复转存副本的头号元凶）**：`core/dedup.py` 的云端复查原先只比对
  散文件、把**文件夹（磁力落盘的主要形态）整体跳过**，导致换一个磁力 hash 的同片资源
  永远被当作「云端没有」→ 重启服务后疯狂复制副本。现在文件与文件夹都会进入同名比对。
- **新增拼音兜底匹配**：云盘文件夹名是拼音/英文（`HeiYeGaoBai`），频道标题是中文
  （《黑夜告白》）时，把中文侧转拼音再精确比对（可选依赖 `pypinyin`，不装自动降级）。
  续集数字不会误吞（《黑夜告白2》不会被《黑夜告白》命中）。
- **修复机器人搜资源「每次结果都一样」**：apibay 引擎不支持中文关键词，收到中文词时
  不搜索、只回全站热门榜（固定几条）。现在 `/s` 收到中文先经免费翻译转英文再搜
  （星际穿越 → Interstellar），翻译失败自动按原词搜索并保留提示。
- 测试补充：文件夹去重、拼音命中、续集不误杀、中文词翻译（`tests/test_dedup_e2e.py`、
  `tests/test_magnet_search.py`），19+13+6 与 9 例全绿。

## v1.2.0（2026-09-04）

- **TG 机器人新增全网磁力搜索**：`/s 关键词` 调用磁力搜索引擎（海盗湾 apibay 公开 API）
  实时搜种子，返回做种人数排序的前 6 条，每行带【存N】按钮**点击即一键转存**；`/saveall 关键词`
  搜到就整批转存。再也不用干等频道更新，想看的片搜一下就有。
- 新模块 `core/magnet_search.py`：多引擎可扩展（当前引擎 `apibay`），统一 `SearchHit`、
  结果去重、按做种人数排序；搜索与频道监听共用同一条网络代理配置（`bot.proxy` 或 `sources.proxy`）。
- 配置新增 `bot.search_enabled` / `bot.search_engines`，Web 面板「TG 机器人」页可开关；
  引擎域名在本机被 DNS 污染，需把 `apibay.org` 真实 IP 写进 `/etc/hosts`（模块头注释说明）。
- 单测：`tests/test_magnet_search.py`（不联网，mock 响应验证解析/去重/排序/配置读写）。

## v1.1.0（2026-09-04）

- **新增 TG 机器人遥控**（`adapters/tgbot.py`，可选，装 `pyTelegramBotAPI` 后在配置 `bot.enabled=true` 开启）：
  - 手机上直接给机器人**发磁力/迅雷/电驴链接即转存**，走与频道监听同一套去重、过滤、自动分类、改名流程；
  - `/status` 查看监听状态与光鸭离线任务进度、`/pause` `/resume` 暂停/恢复、`/add 频道名` 加频道、`/help` 帮助；
  - 转存/跳过/洗版/失败**结果私聊推送**（`bot.notify` 可关）；
  - 权限控制：只响应 `bot.admin_ids` 里的用户，未配置时默认拒绝所有人，防止机器人被陌生人滥用来写你的网盘。
- Web 面板新增**「TG 机器人」设置页**：开关、Token、管理员 ID、结果推送，保存后配置即时写回。
- `config.example.yaml` 增加 `bot:` 配置段与说明；Docker 镜像自带依赖，无需额外安装。
- 配套离线单测 `tests/test_bot_offline.py`（18 例，不联网：配置解析/权限/命令回调/主流程吃 BotMessage）。

## v1.0.7（2026-09-03）

- **修复 `yaml.safe_dump` 的 `sort_keys` 参数**：Python 3.7 的 PyYAML 3.13 不支持该参数，
  调用时抛 `TypeError: dump_all() got an unexpected keyword argument 'sort_keys'`，
  导致保存设置/配置文件时 500 错误。移除参数后恢复正常。
- 修复 `tools/fix_channels.py` 里同样的问题。

## v1.0.6（2026-09-03）

- **修复「自动发现加了频道，面板里却看不到」**：面板（`web/server.py`）与监听程序（`main.py`）是**两个进程**，
  自动发现线程写回了配置文件、也更新了自己进程的内存，但面板进程的内存配置不会变，
  于是页面频道数一直停在旧值（文件里其实早就写进去了，重启面板才看得到）。
  现在面板在读配置的接口里比对文件修改时间，一旦被改动就自动重载，无需重启。
- 重载配置时会先关闭旧的数据库连接，避免反复重载导致 sqlite 连接泄漏。

## v1.0.5（2026-09-03）

对照 **LitePan**（光鸭驱动的逆向实现）与本机 OpenList「光鸭」存储的实际配置，又发现并修复：

- **修复删除文件接口路径不存在**：洗版功能调用的 `/userres/v1/file/delete` 实测返回 **HTTP 404**
  （该接口根本不存在），正确路径是 `/userres/v1/file/delete_file`（body 只需 `fileIds`，不需要 `parentId`）。
  原后果：洗版时**旧版本删不掉、新版本照常转存**，盘里反而多出一份重复文件。
- **删除改为等待异步任务完成**：删除/移动/复制都会返回 `taskId`，现在会轮询
  `/userres/v1/get_task_status` 直到任务结束，避免「旧的还没删完就存新的」。
- 目录列表排序参数对齐 LitePan 与 OpenList 的默认值：`orderBy=3`、`sortType=1`（原为 1/0）。
- 业务接口增加 300ms 最小请求间隔，并识别限流（HTTP 429 / 业务码 354），降低被光鸭限流的概率。
- 补充说明：光鸭的删除默认是**移到回收站**（LitePan `deleteMode` 默认 `trash`），并非立即抹除。
- 另：本机部署时兼容 Python 3.7（路由参数注解 `int | None` 改为 `Optional[int]`）。

## v1.0.4（2026-09-03）

**修复「怎么都登录不上光鸭」的三个真 bug（均已用真实光鸭接口端到端验证）：**

- **修复手动填令牌报 HTTP 501**：`/v1/user/me` 只接受 **GET**，此前用 POST 会被网关在鉴权之前直接拒绝，
  返回 `{"error":"unimplemented","error_code":12,"Method Not Allowed"}` —— 表面看像「令牌校验失败」，
  实际与令牌无关，填什么令牌都是 501。新增 `_get()`，令牌校验改走 GET。
- **修复「令牌总说失效」**：access_token 由光鸭签发、有效期只有 2 小时，从别的工具里粘过来的往往早已过期。
  现在只要填了 refresh_token，就先换发新令牌再校验（刷新成功即登录成功），写入配置的是换新后的令牌；
  只填 refresh_token 也能登录。
- **修复目录列表永远为空（去重与自动分类其实一直没生效）**：光鸭 `get_file_list` 的 `page` 从 **0** 开始计数，
  代码传 `1` 会越过首页，接口只返回 `total` 而没有 `list`，于是程序以为「盘里什么都没有」——
  后果是同一部片子被不同磁力反复转存、分类目录被重复创建。已改为从 0 开始、逐页 +1。
- 二维码图片生成失败不再让整个登录接口 500，前端自动改用授权链接兜底。

## v1.0.3（2026-09-03）

- **真正修复扫码登录「返回不完整」**：根因是账户接口（设备码/登录/刷新令牌）的返回**没有 `success`/`data` 信封、字段在顶层**，
  而此前 `_post` 对所有接口都硬拆 `data` 层，导致 `device_code` 取到 `None` 而报「返回不完整」。
  v1.0.2 只补了请求头、没动这层拆包，所以没修好。本次账户接口改为直接返回原始 JSON（`raw=True`），
  **已用真实光鸭接口端到端验证**：成功拿到 `device_code` 与可点击的 `verification_uri_complete` 链接，并生成二维码 PNG。
- 账户接口补齐 `X-Device-Sign`/`X-Device-Id` 等设备指纹头（对齐 LitePan）。

## v1.0.2（2026-09-03）

- **分类调整**：港台地区不再单独设类，粤语/香港/台湾/TVB 等内容统一并入华语目录
  （港台电影→华语电影、港台剧→国产剧），默认目录对照表同步更新。
- 账户接口补齐设备指纹头（为 v1.0.3 的登录修复铺路）。

## v1.0.1（2026-09-03）

- **修复**：`requirements.txt` 补充 `python-multipart` 依赖。缺失会导致容器启动时
  `/api/backup/restore` 路由注册失败、面板起不来（报错 `Form data requires "python-multipart"`）。
  感谢 ARM 版甲骨文 + 飞牛环境实测发现。

## v1.0.0（2026-09-03）

首个完整可用版本，正式开源发布。

### 核心功能
- **TG 公开频道磁力抓取**：默认网页模式（`t.me/s/频道`），零账号、零风控；可选 userbot 模式监听私有频道/秒级实时。
- **光鸭云盘离线下载**：逆向 LitePan 实现的光鸭 API，磁力/迅雷/电驴直接提交离线下载，不占本地带宽。
- **自动分类转存**：按「内容形态（电影/剧集/动漫/纪录片/综艺/演唱会）+ 地区（华语/日韩/欧美/其他，港台并入华语）」自动建子目录。
- **两级去重**：本地 btih 记录 + 云端按片名复查，杜绝同一片子重复转存七八上十次。
- **集数感知**：追剧/追番时新一集（如第 6 集）不会因去重被误杀，且 `S01E06` 与「第6集」识别为同一集。
- **洗版 / 版本升级**：盘内已有同名同集、新链接质量更优（4K/REMUX/Atmos/HDR…）时自动替换旧版（默认关闭，需手动开启）。
- **关键词/画质过滤**：白黑名单 + 最低分辨率过滤。
- **监控历史**：面板回看每条资源的处理结果（转存/洗版/跳过/失败）与原因。
- **数据持久化 + 备份还原**：所有数据收敛到 `/data` 卷；面板「备份与还原」导出/导入整份数据（仿 OpenList）。
- **频道自动发现**：后台线程从种子文件/URL 抠新频道，自动追加进配置。

### 新增工具（1.0）
- `verify_channels.py`：扫描候选频道公开网页、统计磁力产出，筛出值得监控的频道。
- Docker 化：`Dockerfile` + `docker-compose.yml` + GitHub Actions 自动构建推送 Docker Hub 镜像 `tianjian518/tg-guangya`。
