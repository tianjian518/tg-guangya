# 更新日志

## v1.7.4（2026-09-07）

- **修复：光数字追更词污染 TMDB 查询导致国产剧误判欧美剧**（`早春晴朗更新至17` → 落「欧美剧」且命名残留）。
  两级根因，两级修复：
  1. `_PROGRESS` 进度词只认「更新至…集/话/話/期」结尾，频道实测的「更新至17」（光数字、不带「集」字）
     漏网 → 整串脏词留在 core，落盘名变成 `早春晴朗更新至17`；
  2. 脏 core 整串喂给 TMDB 模糊搜索 → 命中错误条目 original_language=en → region=west 强提示
     压过标题信号 → 国产剧被判进「欧美剧」。
  修复：`_PROGRESS` 新增 `更新至/更新到 + 光数字` 分支（放在带「集/话/期」分支**之后**，
  保证「更新至17集」仍整段剥掉不残尾）；`region_of` 查询前对 core 再剥一遍进度词兜底，
  防未来新追更写法再次污染查询词。
- **不受影响的部分**：kind（剧/影）判定用原始标题，「更新至」信号仍在，剧集不会被误判成电影；
  `is_pack` 看 raw，「更新至」仍判剧集包不生成单集签名。
- 测试：新增 `test_progress_bare_number_strip`（core/folder 剥净、TMDB 查询词干净、zh→国产剧、
  旧带集字形态回归），97 passed。

## v1.7.3（2026-09-07）

- **命名格式变更：年份改用媒体库风格「片名 (年份)」**（Emby/TMDB 标准：英文括号 + 片名间英文空格）。
  旧压制组风格 `小猪佩奇.2004` → 新 `小猪佩奇 (2004)`。覆盖三处输出：
  - 电影：`奥本海默.2023` → `奥本海默 (2023)`；
  - 整季包：`小猪佩奇.2004.S01-S12` → `小猪佩奇 (2004) S01-S12`（analyze 从真实名自带年份时
    也直接产出该格式）；
  - 剧名文件夹：`夏季.2026` → `夏季 (2026)`。
  单集文件名不塞年份（`夏季.S01E04`，年份归剧名文件夹）的既有规则不变。
- **兼容性**：`_norm`/`_norm_full` 归一化剥掉括号与空格，`小猪佩奇.2004` 与 `小猪佩奇 (2004)`
  归一化后同 key —— 旧账本记录、云端旧格式目录的比对**全部不失效**，去重防护照常工作。
- 测试：16 处断言随格式更新（含 dedup_e2e / full_pipeline / dict_titles / noise_strip /
  share_restore）；96 passed。

## v1.7.2（2026-09-07）

- **修复：分享转存的命名源选错（用户纠正：应按分享链接里的真实文件名命名，发帖标题只辅助）**。
  原 `submit_share_one` 用 `_extract_share_title(发帖标题)` 当命名主源，但发帖标题常是频道主引流用的
  营销包装（「小猪佩奇标准命名 全12季」），导致落盘名带垃圾词。改用 `save_share` 返回的
  `entries`（分享内真实文件/文件夹名）作为命名主源，发帖标题仅辅助：
  - 真实名含中文 → 直接用真实名规范化（「小猪佩奇标准命名 全12季」→ `小猪佩奇.S01-S12`）；
  - 真实名是英文压制组命名、标题带中文译名 → 用标题中文名，但**从真实名保留季数签名**
    （`Peppa.Pig.S01-S12` + 「小猪佩奇 (2004)」→ `小猪佩奇.2004.S01-S12`）；
  - 真实名缺年份时从标题补（整季范围插到片名与范围之间：`小猪佩奇.2004.S01-S12`）；
  - 单集/单季签名不塞年份（年份归剧名文件夹）；无效标题退化回真实名。
- 噪声词加资源发布者「整理标签」虚荣词（`标准命名|标准整理|已整理|精品整理|整理版|…`）。
- 新增「全N季」整季包季数范围识别（`全12季` → `S01-S12`，区别于单季 `第2季`→`S02`）。
- 测试：6 个 `submit_share` 测试覆盖命名源逻辑（中文真实名主源 / 英文真实名+中文标题辅助 /
  季数保留 / 年份插位 / 单集不塞年份 / 无效标题退化），96 passed。

## v1.7.1（2026-09-07）

- **修复：命名残留分辨率/编码粘连**（`耳语人 (2026) 2160pH.26515.78 GB` 整段漏剥）。
  `_TECH` 分辨率/编码词要求词边界 `\b`，但 `2160p` 后直接跟 `H`、`H.265` 后直接跟数字时
  两字母/数字间无 `\b`，整段技术参数漏网。改为：分辨率可无分隔粘连编码
  （`2160pH.265` 一体匹配），尾边界用「非字母」而非 `\b`（数字/点/空格照常收尾）。
- **修复：中文译名一律误判华语电影**（`耳语人` 是美国片却落「华语电影」）。
  `analyze()` 对纯中文 core 跳过 TMDB 地区查询，外语片中文译名只能靠「原文有中文→华语」
  兜底。新增 `media_meta.region_of()`（按中文名查 TMDB 拿 original_language→地区，不翻译），
  中文 core 也走这路取 region；**无 TMDB key 时安全回退弱判（行为完全不变）**。
- 测试：test_noise_strip 加粘连剥离用例、test_phrase_region 加中文译名 TMDB 地区用例（mock）。
  全套 94 passed。

## v1.7.0（2026-09-06）

- **新功能：识别光鸭云盘分享链接并转存**（用户需求：机器人能识别光鸭分享链接，
  按标准化命名分门别类落盘）。频道消息 / 机器人 `/add` 里的
  `guangyapan.com/s/<id>`（官方短链）与 `guangyapan.com/share/<id>`（SPA 路由）
  链接现在走**分享转存链路**，不再进离线下载：
  1. **API 链路**（2026-09 从光鸭 Web 前端 bundle 逆向 + 真实分享链接实测）：
     `get_share_summary` → `get_share_access_token`（提取码 ?code= 自动携带）→
     `get_share_page_files_list`（cursor 数字游标翻页，total 判满）→
     `restore_share`（异步任务，status==2 完成）；
  2. **中文命名收纳**（复用离线链路同一套 ident/naming 口径）：转存完成用
     「目录前后差集」定位新条目——单文件电影改名 `片名.年份.ext`；剧集单集
     改名 `片名.SxxExx.ext` 并收进剧名文件夹；多条目合集整批收进一个
     规范命名文件夹；标题解析不出时保持分享原名不炸（兜底名「影视资源」）；
  3. **状态差异化处理**：提取码错误/缺失（209）→ 通知「分享需要提取码」；
     失效（200/201）/过期（202）→ 通知具体原因；无效 shareId（112）→ 参数错误；
  4. **去重键**：`guangya:<shareId>`（同一分享不同 query 写法/两种路径归一键）；
  5. 分享转存不做失败重试（非幂等操作，重试可能产生重复副本）。
- 端到端实测（真实账号 U_0820，落盘根「tg转存」）：资源频道 5 连发真实消息全部成功——
  `国产剧/交锋.S01E03`、`国产剧/囧徒之预演告别.S01E10`、`华语电影/张艺谋电影`、
  `华语电影/李小龙电影`、`华语电影/四骑士.1972`（合计约 88GB，转存后保留在盘）。
- **标题提取**（真实频道消息形态加固）：
  1. 装饰行跳过：`♥♥♥♥♥【国漫】【持续更新，敬请收藏】♥♥♥♥♥` 不再被当成标题——
     `_decorative` 必须分两步剥（`[\W_]` 会连 `【` 一起吞掉，破坏括号段匹配）；
  2. 栏目前缀/引导尾巴剥离：`原盘影视：李小龙电影 复制链接到「光鸭APP」内观看和转存。`
     → 李小龙电影；紧贴链接的「光鸭云盘」推广语、尾部悬挂标点（仙逆，→ 仙逆）一并剥掉；
  3. 追更状态词剥离：`首更至3集` / `更10集` / `4稍后` / `无任何广` / `4KMAX画质` /
     `3G/集中文字幕` / `无广告` / `纯净`（`囧徒之预演告别中文.S01E10` → `囧徒之预演告别.S01E10`）。
- **改名竞态修复**：restore 刚完成时服务端副本元数据不稳定，rename 会静默丢失
  （HTTP 成功但名字没变，18GB 四骑士实测复现）→ `_rename_verified` 改名后立即复核
  目录列表，未生效重试（≤3 轮）。
- 分类词典：26 个国漫 IP 入 ANIME 规则（仙逆/遮天/凡人修仙传/完美世界/吞噬星空等，
  此前被兜底判成电影）。
- 测试：新增 tests/test_share_restore.py 22 例（协议层 FakeApi + 落盘层 ShareSim +
  5 条真实频道消息固化防回归），全套 92 passed。

## v1.4.0（2026-09-04）

- **落盘准入规则（用户明确要求）：必须「全中文规范命名 + 整理归类」，做不到就放弃链接。**
  所有准备落盘（新转存 / 盘里没了重新落盘）的链接，落盘前强制过两道闸，不过 → 不入盘、
  不建目录、直接放弃并通知：
  1. **中文规范命名**：标题必须剥得出中文片名（core 含中文，如 奥本海默 / 流浪地球2 /
     庆余年）。纯英文、纯拼音、无中文名的标题 → 放弃；
  2. **中文归类**：必须开自动整理、且能进明确的中文分类目录（华语电影 / 国产剧 /
     纪录片 / 演唱会 / 综艺 …）。归类被关、或只能进「未分类/其他」这类兜底桶 → 放弃。
  - 新增决策动作 `reject`（放弃链接）：本地记录 status=skipped（保留原因）、通知
    「⛔ 已放弃（无法中文规范命名/归类）」；
  - 新配置开关 `dedup.require_cn`（默认 true）。关闭后回到宽松模式（允许非中文命名与
    兜底归类），供特殊场景使用。
- 判例核对：中文剧/电影/纪录片/演唱会标题全过；`Oppenheimer 2023`、`HeiYeGaoBai.2026`
  等纯英文/拼音标题与未开自动分类时一律放弃。决策测试扩到 34 例全绿（含 3 个准入用例）。

## v1.3.1（2026-09-04）

- **去重判定真源改为「云盘」**（用户明确思路：接到链接 → 规范命名 → 对比云盘 → 已存放弃、
  没存落盘）。v1.3.0 里本地账本命中会「永久跳过」，若盘里文件被手动删/清理后重发同磁力
  就再也转不回来。现在：
  - 同磁力本地已 done + 云盘还能找到 → 跳过；云盘已无（被删/清理）→ **重新落盘**；
  - 任务仍在进行中（submitted）→ 继续等，不重复提交；
  - 账本不再当跳过依据，只做两件事：洗版时给出旧版本的真实质量分、
    判断「已按集收录的剧又来整集包」（防重复整包）。
- **修复精确比对缺陷**：云端 folder 名与规范 key 原来一侧归一化、一侧不归一化（点号/大写
  不参与比较），导致我们自己规范命名的单集文件夹（如 `庆余年.S01E06`）永远精确匹配不上，
  同集换磁力重复会漏判。现在两侧都归一化再比。
- 测试扩到 31 决策（含「本地done但云盘已删→重落盘」「账本有但云盘已删→再转」「submitted
  任务进行中不重复提交」等新用例）+ 15 匹配 + 12 身份识别 + 6 质量评分，全绿。

## v1.3.0（2026-09-04）

- **去重重构：不再靠云盘「猜文本」，转存即建账本**。核心思路：
  1. **转存时规范命名**（新增 `core/ident.py` 统一身份识别）：片名去掉分辨率/编码/语言
     等易变杂质；电影与多集包用「片名.年份」，单集/整季用「片名.SxxExx / .Sxx」。
     同内容无论哪个频道、哪种写法，文件夹名一致 → 判重有了可靠依据。
  2. **落盘成功即记入本地账本**（`titles` 表）：同一部片/同一集只要成功转过一次，
     以后换磁力、换频道、换措辞再来 → 账本命中直接跳过，**不再依赖去云盘逐个比对**。
  3. **追剧/追番不误杀**：集数签名进文件夹与账本 key —— 第6集永远不会被第5集顶掉；
     云端已有「整包目录」（无集号）也不再误杀新出的单集（此前会漏掉后续更新）。
  4. **整包 vs 逐集收录**：已按集追过的剧再遇「全30集」整包 → 跳过（多为重复）。
  5. **洗版更准**：旧版本质量记在账本里，不再从文件夹名猜（新命名不含分辨率）；
     仅当新版本质量更高才替换。启动时自动把历史成功转存回填进账本。
- 老版本行为修复：`1080p/2160p` 里的数字不再被误当成年份；`1-5集` 等范围写法从片名剥净。
- 本地 `tests/test_dedup_e2e.py` 扩到 29 决策 + 15 匹配 + 12 身份识别 + 6 质量评分，全绿。

## v1.2.3（2026-09-04）

- **系统设置新增「TMDB」页**：填写并保存 TMDB API Key（themoviedb.org 免费申请），
  带「测试连接」按钮实时验证 Key 是否有效。保存到 config.yaml 的 `tmdb.api_key`。
  用途：为片名智能识别铺路——后续云端去重可查 TMDB 打通「中文名 ↔ 英文原名」
  （星际穿越 ↔ Interstellar）与续集序号（流浪地球 ↔ 流浪地球2）的关系，纯文本
  认不出的同片不再误判。现在填写不影响现有功能。
- 配置示例 config.example.yaml 同步新增 `tmdb` 段。

## v1.2.2（2026-09-04）

- **纠正 v1.2.1 覆盖不全：单文件同样重复**（不只文件夹）。根因是两类更基础的问题：
  1. **长文件名剥不净**：MoviePilot/压制组自动下载器落盘名如
     `白夜追凶 (2017){tv tmdb-73982}[S01-S02][2160p][HEVC][AAC][中字][2.0](67.7GB 61个文件)`，
     旧的 `title_core` 剥不掉 `{tmdb 标签}`/`[S01-S02]`/体积与文件数尾巴，核心名残留一长串
     数字字母 → `names_match` 把同一部片判成不同片 → 云端复查查不到 → 单文件/文件夹都复制副本。
     现在标题归一化会先剥掉这类结构性尾巴再比对。
  2. **复查只查分类目录**：分类目录下没有时，从不去查**转存根目录**——旧版本/未分类/其它
     工具平铺在根目录的资源永远看不见，照样重转。现在云端复查 = 分类目录 + 转存根目录都查。
- 测试新增：MoviePilot 长名文件夹/单文件在根目录、同片新磁力 → 跳过（21 e2e + 15 匹配 + 6 评分全绿）。

## v1.2.1（2026-09-04）

- **修复云端去重形同虚设（重复转存副本的头号元凶）**：`core/dedup.py` 的云端复查原先只比对
  散文件、把**文件夹（磁力落盘的主要形态）整体跳过**，导致换一个磁力 hash 的同片资源
  永远被当作「云端没有」→ 重启服务后疯狂复制副本。现在文件与文件夹都会进入同名比对。
- **新增拼音兜底匹配**：云盘文件夹名是拼音/英文（`HeiYeGaoBai`），频道标题是中文
  （《黑夜告白》）时，把中文侧转拼音再精确比对（可选依赖 `pypinyin`，不装自动降级）。
  续集数字不会误吞（《黑夜告白2》不会被《黑夜告白》命中）。
- **修复机器人搜资源「每次结果都一样」**：apibay 引擎不支持中文关键词，收到中文词时
  不搜索、只回全站热门榜（固定几条）。现在 `/s` 收到中文先经免费翻译转英文再搜
  （星际穿越 → Interstellar），翻译失败自动按原词搜索并保留提示。
- 测试补充：文件夹去重、拼音命中、续集不误杀、中文词翻译（`tests/test_dedup_e2e.py`、
  `tests/test_magnet_search.py`），19+13+6 与 9 例全绿。

## v1.2.0（2026-09-04）

- **TG 机器人新增全网磁力搜索**：`/s 关键词` 调用磁力搜索引擎（海盗湾 apibay 公开 API）
  实时搜种子，返回做种人数排序的前 6 条，每行带【存N】按钮**点击即一键转存**；`/saveall 关键词`
  搜到就整批转存。再也不用干等频道更新，想看的片搜一下就有。
- 新模块 `core/magnet_search.py`：多引擎可扩展（当前引擎 `apibay`），统一 `SearchHit`、
  结果去重、按做种人数排序；搜索与频道监听共用同一条网络代理配置（`bot.proxy` 或 `sources.proxy`）。
- 配置新增 `bot.search_enabled` / `bot.search_engines`，Web 面板「TG 机器人」页可开关；
  引擎域名在本机被 DNS 污染，需把 `apibay.org` 真实 IP 写进 `/etc/hosts`（模块头注释说明）。
- 单测：`tests/test_magnet_search.py`（不联网，mock 响应验证解析/去重/排序/配置读写）。

## v1.1.0（2026-09-04）

- **新增 TG 机器人遥控**（`adapters/tgbot.py`，可选，装 `pyTelegramBotAPI` 后在配置 `bot.enabled=true` 开启）：
  - 手机上直接给机器人**发磁力/迅雷/电驴链接即转存**，走与频道监听同一套去重、过滤、自动分类、改名流程；
  - `/status` 查看监听状态与光鸭离线任务进度、`/pause` `/resume` 暂停/恢复、`/add 频道名` 加频道、`/help` 帮助；
  - 转存/跳过/洗版/失败**结果私聊推送**（`bot.notify` 可关）；
  - 权限控制：只响应 `bot.admin_ids` 里的用户，未配置时默认拒绝所有人，防止机器人被陌生人滥用来写你的网盘。
- Web 面板新增**「TG 机器人」设置页**：开关、Token、管理员 ID、结果推送，保存后配置即时写回。
- `config.example.yaml` 增加 `bot:` 配置段与说明；Docker 镜像自带依赖，无需额外安装。
- 配套离线单测 `tests/test_bot_offline.py`（18 例，不联网：配置解析/权限/命令回调/主流程吃 BotMessage）。

## v1.0.7（2026-09-03）

- **修复 `yaml.safe_dump` 的 `sort_keys` 参数**：Python 3.7 的 PyYAML 3.13 不支持该参数，
  调用时抛 `TypeError: dump_all() got an unexpected keyword argument 'sort_keys'`，
  导致保存设置/配置文件时 500 错误。移除参数后恢复正常。
- 修复 `tools/fix_channels.py` 里同样的问题。

## v1.0.6（2026-09-03）

- **修复「自动发现加了频道，面板里却看不到」**：面板（`web/server.py`）与监听程序（`main.py`）是**两个进程**，
  自动发现线程写回了配置文件、也更新了自己进程的内存，但面板进程的内存配置不会变，
  于是页面频道数一直停在旧值（文件里其实早就写进去了，重启面板才看得到）。
  现在面板在读配置的接口里比对文件修改时间，一旦被改动就自动重载，无需重启。
- 重载配置时会先关闭旧的数据库连接，避免反复重载导致 sqlite 连接泄漏。

## v1.0.5（2026-09-03）

对照 **LitePan**（光鸭驱动的逆向实现）与本机 OpenList「光鸭」存储的实际配置，又发现并修复：

- **修复删除文件接口路径不存在**：洗版功能调用的 `/userres/v1/file/delete` 实测返回 **HTTP 404**
  （该接口根本不存在），正确路径是 `/userres/v1/file/delete_file`（body 只需 `fileIds`，不需要 `parentId`）。
  原后果：洗版时**旧版本删不掉、新版本照常转存**，盘里反而多出一份重复文件。
- **删除改为等待异步任务完成**：删除/移动/复制都会返回 `taskId`，现在会轮询
  `/userres/v1/get_task_status` 直到任务结束，避免「旧的还没删完就存新的」。
- 目录列表排序参数对齐 LitePan 与 OpenList 的默认值：`orderBy=3`、`sortType=1`（原为 1/0）。
- 业务接口增加 300ms 最小请求间隔，并识别限流（HTTP 429 / 业务码 354），降低被光鸭限流的概率。
- 补充说明：光鸭的删除默认是**移到回收站**（LitePan `deleteMode` 默认 `trash`），并非立即抹除。
- 另：本机部署时兼容 Python 3.7（路由参数注解 `int | None` 改为 `Optional[int]`）。

## v1.0.4（2026-09-03）

**修复「怎么都登录不上光鸭」的三个真 bug（均已用真实光鸭接口端到端验证）：**

- **修复手动填令牌报 HTTP 501**：`/v1/user/me` 只接受 **GET**，此前用 POST 会被网关在鉴权之前直接拒绝，
  返回 `{"error":"unimplemented","error_code":12,"Method Not Allowed"}` —— 表面看像「令牌校验失败」，
  实际与令牌无关，填什么令牌都是 501。新增 `_get()`，令牌校验改走 GET。
- **修复「令牌总说失效」**：access_token 由光鸭签发、有效期只有 2 小时，从别的工具里粘过来的往往早已过期。
  现在只要填了 refresh_token，就先换发新令牌再校验（刷新成功即登录成功），写入配置的是换新后的令牌；
  只填 refresh_token 也能登录。
- **修复目录列表永远为空（去重与自动分类其实一直没生效）**：光鸭 `get_file_list` 的 `page` 从 **0** 开始计数，
  代码传 `1` 会越过首页，接口只返回 `total` 而没有 `list`，于是程序以为「盘里什么都没有」——
  后果是同一部片子被不同磁力反复转存、分类目录被重复创建。已改为从 0 开始、逐页 +1。
- 二维码图片生成失败不再让整个登录接口 500，前端自动改用授权链接兜底。

## v1.0.3（2026-09-03）

- **真正修复扫码登录「返回不完整」**：根因是账户接口（设备码/登录/刷新令牌）的返回**没有 `success`/`data` 信封、字段在顶层**，
  而此前 `_post` 对所有接口都硬拆 `data` 层，导致 `device_code` 取到 `None` 而报「返回不完整」。
  v1.0.2 只补了请求头、没动这层拆包，所以没修好。本次账户接口改为直接返回原始 JSON（`raw=True`），
  **已用真实光鸭接口端到端验证**：成功拿到 `device_code` 与可点击的 `verification_uri_complete` 链接，并生成二维码 PNG。
- 账户接口补齐 `X-Device-Sign`/`X-Device-Id` 等设备指纹头（对齐 LitePan）。

## v1.0.2（2026-09-03）

- **分类调整**：港台地区不再单独设类，粤语/香港/台湾/TVB 等内容统一并入华语目录
  （港台电影→华语电影、港台剧→国产剧），默认目录对照表同步更新。
- 账户接口补齐设备指纹头（为 v1.0.3 的登录修复铺路）。

## v1.0.1（2026-09-03）

- **修复**：`requirements.txt` 补充 `python-multipart` 依赖。缺失会导致容器启动时
  `/api/backup/restore` 路由注册失败、面板起不来（报错 `Form data requires "python-multipart"`）。
  感谢 ARM 版甲骨文 + 飞牛环境实测发现。

## v1.0.0（2026-09-03）

首个完整可用版本，正式开源发布。

### 核心功能
- **TG 公开频道磁力抓取**：默认网页模式（`t.me/s/频道`），零账号、零风控；可选 userbot 模式监听私有频道/秒级实时。
- **光鸭云盘离线下载**：逆向 LitePan 实现的光鸭 API，磁力/迅雷/电驴直接提交离线下载，不占本地带宽。
- **自动分类转存**：按「内容形态（电影/剧集/动漫/纪录片/综艺/演唱会）+ 地区（华语/日韩/欧美/其他，港台并入华语）」自动建子目录。
- **两级去重**：本地 btih 记录 + 云端按片名复查，杜绝同一片子重复转存七八上十次。
- **集数感知**：追剧/追番时新一集（如第 6 集）不会因去重被误杀，且 `S01E06` 与「第6集」识别为同一集。
- **洗版 / 版本升级**：盘内已有同名同集、新链接质量更优（4K/REMUX/Atmos/HDR…）时自动替换旧版（默认关闭，需手动开启）。
- **关键词/画质过滤**：白黑名单 + 最低分辨率过滤。
- **监控历史**：面板回看每条资源的处理结果（转存/洗版/跳过/失败）与原因。
- **数据持久化 + 备份还原**：所有数据收敛到 `/data` 卷；面板「备份与还原」导出/导入整份数据（仿 OpenList）。
- **频道自动发现**：后台线程从种子文件/URL 抠新频道，自动追加进配置。

### 新增工具（1.0）
- `verify_channels.py`：扫描候选频道公开网页、统计磁力产出，筛出值得监控的频道。
- Docker 化：`Dockerfile` + `docker-compose.yml` + GitHub Actions 自动构建推送 Docker Hub 镜像 `tianjian518/tg-guangya`。
